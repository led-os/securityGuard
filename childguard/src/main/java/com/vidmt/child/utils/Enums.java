package com.vidmt.child.utils;

public class Enums {
	public enum CmdType {
		ALARM, REMOTE_AUDIO, HIDE_ICON, SHOW_ICON
	}

	public enum MapType {
		MAIN, EFENCE, TRACE
	}

	public enum VipFuncType {
		REMOTE_AUDIO, ALARM, NAV, TRACE, NO_AD, HIDE_ICON, SPORT
	}

	public enum PayType {
		ALI, WX, MM
	}
}
