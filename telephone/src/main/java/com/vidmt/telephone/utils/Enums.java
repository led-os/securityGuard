package com.vidmt.telephone.utils;

public class Enums {
	public static enum AccType {
		PHONE, QQ, SINA, EMAIL
	}

	public static enum MapType {
		MAIN, EFENCE, TRACE
	}

	public static enum ResType {
		AVATAR, ALBUM
	}

	public static enum AddFriendType {// 添加好友方式类型
		ALREADY_FRIEND, DIRECT, WAIT, VIP_WAIT, NOT_ALLOWED, NORMAL_WAIT
	}

	public static enum VipType {
		YEAR, TRY
	}

	public static enum PayType {
		WX, ALI, MM
	}
}
