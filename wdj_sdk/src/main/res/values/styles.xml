<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="InstallButton">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:background">@drawable/install_button</item>
        <item name="android:text">@string/install</item>
    </style>

    <style name="Activity">
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="InterstitialActivity" parent="Activity">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <style name="roomRatingBar" parent="@android:style/Widget.RatingBar">
        <item name="android:progressDrawable">@drawable/appwall_ratingbar_star</item>
        <item name="android:minHeight">15dp</item>
        <item name="android:maxHeight">15dp</item>
    </style>
</resources>